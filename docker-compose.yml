# ==============================================================================
# OpenClaw Docker Compose Development Environment
# ==============================================================================
# Quick Start:
#   ./docker-dev-setup.sh                              # 启动服务
#   docker compose -f docker-compose.dev.yml logs -f   # 查看日志
#   docker compose -f docker-compose.dev.yml down      # 停止服务
#
# Key Features:
#   - Claude Code CLI 内置
#   - 代理配置 (访问 Google/Claude API)
#   - Go/TypeScript/Python 开发环境
#   - 浏览器自动化 (Playwright)
#   - Office 文件处理 (pandoc + LaTeX)
# ==============================================================================

# ------------------------------------------------------------------------------
# YAML Anchors for DRY Configuration
# ------------------------------------------------------------------------------
x-openclaw-common: &openclaw-common
  image: ${OPENCLAW_IMAGE:-openclaw}
  environment: &common-env
    HOST_UID: ${HOST_UID:-1000}
    # ─────────────────────────────────────────────────────────────────
    # Proxy Configuration
    # ─────────────────────────────────────────────────────────────────
    # Required for accessing Google and Claude API from restricted network environments
    # Ports: 7897 (HTTP proxy), 15721 (Claude API proxy)
    # ─────────────────────────────────────────────────────────────────
    HTTP_PROXY: ${HTTP_PROXY:-}
    HTTPS_PROXY: ${HTTPS_PROXY:-}
    NO_PROXY: localhost,127.0.0.1,host.docker.internal,.local,.internal
    # ─────────────────────────────────────────────────────────────────
    # Claude API Configuration
    # ─────────────────────────────────────────────────────────────────
    ANTHROPIC_AUTH_TOKEN: ${ANTHROPIC_AUTH_TOKEN:-}
    ANTHROPIC_BASE_URL: ${ANTHROPIC_BASE_URL:-}
    API_TIMEOUT_MS: "3000000"
    CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC: "1"
    CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS: "1"
    # ─────────────────────────────────────────────────────────────────
    # Runtime Configuration
    # ─────────────────────────────────────────────────────────────────
    PLAYWRIGHT_BROWSERS_PATH: /home/node/.cache/ms-playwright
    HOME: /home/node
    TERM: xterm-256color
  extra_hosts:
    - "host.docker.internal:host-gateway"
  logging: &common-logging
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"
  deploy: &common-deploy
    resources:
      limits:
        memory: 4G
        cpus: '4.0'
      reservations:
        memory: 1G
        cpus: '0.5'
  restart: unless-stopped

# ------------------------------------------------------------------------------
# Named Volumes
# ------------------------------------------------------------------------------
volumes:
  openclaw-node-modules:
    name: openclaw-node-modules
  openclaw-go-mod:
    name: openclaw-go-mod
  openclaw-playwright-cache:
    name: openclaw-playwright-cache
  openclaw-playwright-bin:
    name: openclaw-playwright-bin
  # Unified state storage for sessions, identities, and logs
  openclaw-state:
    name: openclaw-state
  # Team skills storage (bind mount to host directory)
  openclaw-team-skills:
    name: openclaw-team-skills
    driver: local
    driver_opts:
      type: none
      device: ${OPENCLAW_TEAM_SKILLS_DIR}
      o: bind

services:
  # ============================================================================
  # OpenClaw Gateway
  # ============================================================================
  openclaw-init:
    <<: *openclaw-common
    user: "0:0"
    container_name: openclaw-init
    # Don't restart - this is a one-shot job
    restart: "no"
    # Run before gateway to fix config issues (unhealthy from old schema)
    command: ["sh", "-c", "echo '==> Pre-checking configuration...' && openclaw doctor --fix && echo '==> Configuration ready'"]
    volumes:
      # === 必需挂载 (Required) ===
      # 使用 .env 中定义的变量 (OPENCLAW_CONFIG_DIR, OPENCLAW_CUSTOM_SKILLS_DIR, OPENCLAW_TEAM_SKILLS_DIR)
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw:rw
      - ${OPENCLAW_CUSTOM_SKILLS_DIR}:/home/node/skills/custom-skills:rw
      # Team skills: named volume with bind mount target
      - openclaw-team-skills:/home/node/skills/team-skills:rw
      - openclaw-state:/home/node/.openclaw-state:rw
    healthcheck:
      test: ["CMD", "sh", "-c", "test -f /home/node/.openclaw/openclaw.json"]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 10s

  openclaw-gateway:
    <<: *openclaw-common
    user: "0:0" # Run as root to fix permissions - enabled to fix EACCES
    container_name: openclaw-gateway
    depends_on:
      openclaw-init:
        condition: service_completed_successfully
    ports:
      - "127.0.0.1:${OPENCLAW_GATEWAY_PORT:-18789}:18789"
      - "127.0.0.1:${OPENCLAW_BRIDGE_PORT:-18790}:18790"
    env_file:
      - .env
    init: true
    volumes:
      # === 必需挂载 (Required) ===
      # 使用 .env 中定义的变量 (OPENCLAW_CONFIG_DIR, OPENCLAW_WORKSPACE_DIR, OPENCLAW_CUSTOM_SKILLS_DIR, OPENCLAW_TEAM_SKILLS_DIR)
      # OpenClaw config seed (mount host dir to separate path)
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw-seed:ro
      # Workspace (AI working directory - required!)
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace:rw
      # Custom skills directory (user-defined skills)
      - ${OPENCLAW_CUSTOM_SKILLS_DIR}:/home/node/skills/custom-skills:rw
      # Team skills: named volume with bind mount target
      - openclaw-team-skills:/home/node/skills/team-skills:rw
      # Unified state volume for persistent container data
      - openclaw-state:/home/node/.openclaw:rw
      # Mount local entrypoint to apply fixes without rebuilding
      - ./docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh:ro

      # === 可选挂载 (Optional) - 根据需求选择 ===
      # Claude Code session state (shared with host)
      - ${HOME}/.claude:/home/node/.claude:rw
      # Named volumes for caches (optional, speeds up restarts)
      - openclaw-node-modules:/app/node_modules:rw
      - openclaw-go-mod:/home/node/go/pkg/mod:rw
      - openclaw-playwright-cache:/home/node/.cache/ms-playwright:rw
      # -------------------------------------------------------------------------
      # 🔌 添加额外挂载点 (Extend with custom mounts)
      # -------------------------------------------------------------------------
      # 示例 (Examples):
      #   - /your/project/path:/home/node/projects:rw
      #   - /Users/yourname/Downloads:/home/node/Downloads:rw
      #   - /shared/team-config:/home/node/team-config:ro
      #
      # 格式: 宿主机路径:容器内路径:访问模式 (rw=读写, ro=只读)
      # Format: host_path:container_path:access_mode (rw=read-write, ro=read-only)
      #
      # 修改后执行: make down && make up
      # After modifying: make down && make up
      # -------------------------------------------------------------------------
      # 移除复杂的 inline command，改用容器内置 entrypoint
      # command: ["openclaw", "gateway", "--allow-unconfigured"]
    healthcheck: &common-healthcheck
      test: [ "CMD", "node", "-e", "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      - "openclaw.service=gateway"
      - "openclaw.environment=dev"

  # ============================================================================
  # OpenClaw CLI
  # ============================================================================
  openclaw-cli:
    <<: *openclaw-common
    user: "0:0" # 同步 gateway，允许 entrypoint 修复权限
    container_name: openclaw-cli
    depends_on:
      openclaw-gateway:
        condition: service_healthy
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      <<: *common-env
      BROWSER: echo
    init: true
    volumes:
      # === CLI 容器只需最基本的挂载 ===
      # 使用 .env 中定义的变量 (OPENCLAW_CONFIG_DIR, OPENCLAW_WORKSPACE_DIR, OPENCLAW_CUSTOM_SKILLS_DIR, OPENCLAW_TEAM_SKILLS_DIR)
      # OpenClaw config seed (mount host dir to separate path)
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw-seed:ro
      # Custom skills directory (user-defined skills)
      - ${OPENCLAW_CUSTOM_SKILLS_DIR}:/home/node/skills/custom-skills:rw
      # Team skills: named volume with bind mount target
      - openclaw-team-skills:/home/node/skills/team-skills:rw
      # Unified state volume for persistent container data
      - openclaw-state:/home/node/.openclaw:rw
    stdin_open: true
    tty: true
    # CLI 是交互式容器，不需要健康检查
    healthcheck:
      disable: true
    # CLI 容器现在默认进入交互模式或由 Makefile 控制
    command: [ "tail", "-f", "/dev/null" ]
    labels:
      - "openclaw.service=cli"
      - "openclaw.environment=dev"
